import * as React from 'react';

export interface AppProps {}

/** Full OES Global corporate site: Header, Hero, BrandFamily, ValueSection, Footer, with the brand-detail overlay wired up. */
export declare function App(props?: AppProps): React.ReactElement;
